/**
 * Facade du module Ratios -- re-exporte depuis ratios/.
 * @module aggregations/ratios
 */

export type { RatioValue, RatioRow, RatiosData } from "./ratios/types";
export { buildRatiosRows } from "./ratios/build-rows";
