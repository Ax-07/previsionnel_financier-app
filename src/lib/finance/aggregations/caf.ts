/**
 * Facade du module CAF -- re-exporte depuis caf/.
 * @module aggregations/caf
 */

export type { CafRowValue, CafRow, CafData } from "./caf/types";
export { buildCafRows } from "./caf/build-rows";
